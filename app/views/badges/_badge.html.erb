<% if badge.active?%>
<h4><u><%=badge.title%></u><h4>
  
<%= image_tag badge.image %>

<% if badge.rule == "category"%>
  <h5>Выдается за успешное прохождение всеx тестов категории: <%=badge.rule_value%></h5>
<% else %>
  <% if badge.rule == "level" %>
    <h5>Выдается за успешное прохождение всеx тестов уровня: <%=badge.rule_value%></h5>
  <% else %>
    <h5>Выдается за успешное прохождение теста с первой попытки.</h5>
  <% end %>
<%end%>
<% end %> 

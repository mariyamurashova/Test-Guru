<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
       <b><%= t('.welcome', user: current_user.email) %> | </b>
        <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('.log_in'), new_session_path(:user) %> |
      <%= link_to t('.sign_up'), new_registration_path(:user) %> 
   <% end %>
   <b><%= link_to "Contact Us",  new_contact_path %></b>
   </span>
</nav>
